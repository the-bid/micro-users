type Query{
  users(filter: String): [User!]!
  user(id:ID!): User!
  getJWT: Token!
}

type User {
  id: ID!
  username: String!
  createdAt: Date!
}

type Token {
  token: String!
  expiresIn: Int!
}

scalar Date
