type Query{
  users(filter: String): [User!]!
  user(id:ID!): User!
  getJWT: Token!
}

type User {
  id: ID!
  username: String!
  createdAt: DateTime!
}

type Token {
  token: String!
  expiresIn: Int!
}

scalar DateTime
